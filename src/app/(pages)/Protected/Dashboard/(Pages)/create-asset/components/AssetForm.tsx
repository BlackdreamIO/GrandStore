"use client"

import { useForm, SubmitHandler } from "react-hook-form"

export const AssetForm = () => {
    
    const {  } = useForm();

    return (
        <div className="w-full">
            <form action="#">

            </form>
        </div>
    )
}
