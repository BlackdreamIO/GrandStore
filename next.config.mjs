/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assetstorev1-prd-cdn.unity3d.com', 'i.ytimg.com', 'unityassets4free.com'],
    },
};

export default nextConfig;
